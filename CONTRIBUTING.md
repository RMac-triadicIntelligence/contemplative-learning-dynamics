# Contributing

PRs welcome. If you add features, please include:
- a short rationale in the PR description
- a minimal test in `tests/`
- updated README notes (if behavior changes)

## Dev setup
```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
